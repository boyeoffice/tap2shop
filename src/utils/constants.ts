import dotenv from 'dotenv';

dotenv.config({ path: '../../.env' });

/* Database Config */
export const DB_NAME = 'tap2shop';
export const DB_PORT = 4406;
export const DB_USER = 'tap';
export const DB_PASSWORD = 'password';
export const DB_HOST = '127.0.0.1';
